#include "session_router.h"
#include "../session/session.h"

void session_router(Session *s) {
    switch (s->proto) {
        case 1:
            break;
        case 2:
            break;
        case 3:
            break;
        default:
            break;
    }
}
